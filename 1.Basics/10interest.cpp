#include<iostream>
using namespace std;

int main (){
    float P, R, T;
    cout<<"Enter P : ";
    cin >> P;
    cout<<"Enter R : ";
    cin >> R;
    cout<<"Enter T : ";
    cin >> T;

    cout<<"SI = "<<(P*R*T)/100 << endl;

    return 0;
}